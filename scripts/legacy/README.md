# Quarantined — superseded by project_mai_tai.backtest

These throwaway backtests had the bugs the validated engine was built to prevent (fake-win exit, phantom re-entries, optimistic fills). Kept for reference only. Do NOT extend them — use the engine (see src/project_mai_tai/backtest/README.md).
